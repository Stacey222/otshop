# Scripts

Reserved for explicit setup and operational scripts. `pnpm dev:doctor` is read-only and reports safe local application, executable, migration, database, and administrator-bootstrap readiness categories. `pnpm auth:bootstrap` remains the explicit one-time administrator creation command. Neither script provisions PostgreSQL, changes database authentication, or contains credentials.
